@model Application.DTO.UnitDto
@{
    ViewData["Title"] = "Удаление единицы измерения";
}

<div class="container mt-5">
    <div class="card shadow-sm">
        <div class="card-header bg-light">
            <h5 class="card-title mb-0">@ViewData["Title"]</h5>
        </div>
        <div class="card-body">
            <h6 class="text-danger">Вы уверены, что хотите удалить эту единицу измерения?</h6>

            <div class="alert alert-warning mt-4">
                <h6 class="alert-heading">Внимание!</h6>
                <p class="mb-0">Удаление единицы измерения может повлиять на связанные ресурсы и документы.</p>
            </div>

            <dl class="row mt-4">
                <dt class="col-sm-3">ID:</dt>
                <dd class="col-sm-9">@Model.Id</dd>

                <dt class="col-sm-3">Наименование:</dt>
                <dd class="col-sm-9">@Model.Name</dd>

                <dt class="col-sm-3">Состояние:</dt>
                <dd class="col-sm-9">
                    <span class="badge bg-@(Model.State == "Active" ? "success" : "secondary")">
                        @Model.State
                    </span>
                </dd>
            </dl>

            <form asp-action="Delete">
                <input type="hidden" asp-for="Id" />
                <div class="d-flex justify-content-between mt-4">
                    <a asp-action="Index" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left"></i> Отменить
                    </a>
                    <button type="submit" class="btn btn-danger">
                        <i class="bi bi-trash"></i> Удалить
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<style>
    .card {
        border-radius: 10px;
    }

    dt {
        font-weight: 500;
    }

    .alert-warning {
        border-left: 4px solid #ffc107;
    }
</style>